#include "main.h"

/**
 * _strcmp - compares two strings
 * @s1: first string
 * @s1: second string
 *
 * Return: 1 if strings match, negative if second string
 * is larger, positive, otherwise
 */
int _strcmp(char *s1, char *s2)
{
	return (1);
}
